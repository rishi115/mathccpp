#include<iostream>
#include<queue>

classs node*{
    public:
        int d;
        node* left;
        node* right;
        
    node*{
        this->data=d;
        this->left=NULL;
        this->right=NULL;
    }
};

node* buildtree(node* root){
    int data;
    cin<<data;
    node=new node(data);
    if(data==NULL){
        
    }
}